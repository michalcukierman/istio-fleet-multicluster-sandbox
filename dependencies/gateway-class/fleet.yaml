name: gateway-class

labels:
  streamx.com/dependency: gateway-class

defaultNamespace: envoy-gateway-system

overrideTargets:
  - clusterSelector:
      matchExpressions:
        - key: streamx.com/cluster-role
          operator: In
          values:
            - edge
            - processing