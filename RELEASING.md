# Releasing

To create a new release:

1. Open [Create a new release](https://github.com/streamx-com/streamx-clustermesh-sandbox/releases/new).
2. Create a new tag in version format, for example: `0.0.1`.
3. Publish the release.